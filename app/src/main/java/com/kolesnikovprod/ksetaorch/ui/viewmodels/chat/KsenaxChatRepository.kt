package com.kolesnikovprod.ksetaorch.ui.viewmodels.chat

/**
 * Временный совместимый путь к chat-контракту.
 *
 * Новому коду следует импортировать контракт из `chat.domain`. Alias оставлен,
 * чтобы пока не менять созданные chat-ViewModel.
 */
typealias KsenaxChatRepository =
    com.kolesnikovprod.ksetaorch.storage.chat.domain.KsenaxChatRepository
